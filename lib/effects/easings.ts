/** Shared Motion easing curves for the control-center subsystem. */

/** Surface spring: fast settle, slight overshoot dampened — matches macOS sheet feel. */
export const easeSurface = [0.16, 1, 0.3, 1] as const
